---
title: leetcode3.无重复字符的最长子串
date: '2023-03-07'
author: serrini
showToc: true
TocOpen: false
draft: false
tags:
- leetcode
- 滑动窗口
- 双指针
categories:
- LeetCode
---
## Question-3

>
>给定一个字符串 s ，请你找出其中不含有重复字符的 最长子串 的长度。 
>
>
>
> 示例 1: 
>
>
>输入: s = "abcabcbb"
>输出: 3 
>解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。
>
>
> 示例 2: 
>
>
>输入: s = "bbbbb"
>输出: 1
>解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。
>
>
> 示例 3: 
>
>
>输入: s = "pwwkew"
>输出: 3
>解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
>     请注意，你的答案必须是 子串 的长度，"pwke" 是一个子序列，不是子串。
>
>
>
>
> 提示： 
>
>
> 0 <= s.length <= 5 * 10⁴ 
> s 由英文字母、数字、符号和空格组成 
>
>
> Related Topics 哈希表 字符串 滑动窗口 👍 8835 👎 0


## Answer-3

```cpp
/*
 * 3
 */
int lengthOfLongestSubstring_func1(string s) {
    int start = 0, ans = 0;
    unordered_set<char> strset; //滑动窗口，临时子串
    for (int i = 0; i < s.size(); i++) {
        while (strset.find(s[i]) != strset.end()){ //有重复
            strset.erase(s[start]); //窗口右移
            start++;
        }
        ans = max(ans, i-start+1);
        strset.insert(s[i]); //set中没该字符，加入窗口
    }
    return ans;
}

int lengthOfLongestSubstring_func2(string s) {
    int start = 0, end = 0, ans = 0; //双指针
    unordered_set<char> strset;
    while (end < s.size()) {
        if (strset.find(s[end]) != strset.end()) { //有重复
            strset.erase(s[start]); //左指针移动，删除..
            start++;
        } else { //无重复
            strset.insert(s[end]); //右指针移动，加入..
            end++;
        }
        if (strset.size() > ans) ans = strset.size();
//        ans = max(ans, strset.size());
    }
    return ans;
}
```

## Attention

1. 方法一，滑动窗口
   1. unordered_set保存临时子串，while循环当当前字符有重复时，窗口右移动，起始指针右移，在set中移除该字符；当前字符无重复时，将当前字符加入窗口
   2. 返回结果长度在当前字符无重复时更新
2. 方法二，双指针
   1. end指针移动，while循环
   2. 判断当前字符有重复时，左指针右移动，删除start指向的字符；无重复，当前字符加入set，右指针继续右移动
